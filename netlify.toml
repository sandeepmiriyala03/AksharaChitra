[build]
  functions = "netlify/functions"  # Use standard functions folder without leading dot
  publish = "."                    # Change this if your build output folder is different

[dev]
  port = 8888                     # Local development port

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
