[build]
  functions = "netlify/functions"      # Correct functions folder path
  publish = "."                        # Correct if your static files (e.g., index.html) are in project root

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"    # Correct use of the leading dot for function paths
  status = 200
